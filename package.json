{
  "name": "laravel-log-watcher",
  "displayName": "laravel-log-watcher",
  "description": "Watches Laravel logs and notifies on changes",
  "version": "0.0.1",
  "publisher": "azmolla",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Laravel Log Watcher",
      "properties": {
        "laravelLogWatcher.logFilePath": {
          "type": "string",
          "default": "storage/logs/laravel.log",
          "description": "Path to the Laravel log file (relative or absolute)"
        },
        "laravelLogWatcher.notificationMessage": {
          "type": "string",
          "default": "Laravel log updated!",
          "description": "Notification message when the log file changes"
        },
        "laravelLogWatcher.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable Laravel log watcher"
        }
      }
    },
    "commands": [
      {
        "command": "laravelLogWatcher.toggle",
        "title": "Toggle Laravel Log Watcher"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.102.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
