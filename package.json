{
  "name": "laravel-log-watcher",
  "displayName": "Laravel Log Watcher",
  "description": "Watches Laravel logs and notifies on changes",
  "version": "0.0.3",
  "publisher": "azmolla",
  "repository": {
    "type": "git",
    "url": "https://github.com/AbiruzzamanMolla/laravel-log-watcher.git"
  },
  "bugs": {
    "url": "https://github.com/AbiruzzamanMolla/laravel-log-watcher/issues"
  },
  "homepage": "https://marketplace.visualstudio.com/items?itemName=azmolla.laravel-log-watcher",
  "engines": {
    "vscode": "^1.85.0"
  },
  "icon": "icon.png",
  "categories": [
    "Other",
    "Programming Languages"
  ],
  "keywords": [
    "laravel",
    "log",
    "log-alert",
    "php"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:laravelLogWatcher.toggle"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Laravel Log Watcher",
      "properties": {
        "laravelLogWatcher.logFilePath": {
          "type": "string",
          "default": "storage/logs/laravel.log",
          "description": "Path to the Laravel log file (relative to workspace root or absolute)"
        },
        "laravelLogWatcher.notificationMessage": {
          "type": "string",
          "default": "New log fired in laravel log file!",
          "description": "Notification message to show when the log file changes"
        },
        "laravelLogWatcher.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable the Laravel log watcher"
        }
      }
    },
    "commands": [
      {
        "command": "laravelLogWatcher.toggle",
        "title": "Toggle Laravel Log Watcher"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "lint": "eslint src --ext .ts",
    "pretest": "npm run compile && npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.11.30",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.0.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2"
  }
}
